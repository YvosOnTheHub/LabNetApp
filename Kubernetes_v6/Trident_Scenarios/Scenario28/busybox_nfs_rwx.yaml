apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mydata-nfs-rwx
  namespace: scenario28
  labels:
    app.kubernetes.io/name: scenario28
    app: bbox-nfs-rwx
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: storage-class-nfs
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: busybox-nfs-rwx
  namespace: scenario28
  labels:
    app.kubernetes.io/name: scenario28
    app: bbox-nfs-rwx
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: scenario28
      app: bbox-nfs-rwx
  template:
    metadata:
      labels:
        app.kubernetes.io/name: scenario28
        app: bbox-nfs-rwx
    spec:
      nodeSelector:
        kubernetes.io/os: linux
      containers:
      - name: busybox
        image: registry.demo.netapp.com/busybox:1.35.0
        imagePullPolicy: IfNotPresent
        command: ['sleep', '3600']        
        volumeMounts:
        - mountPath: /data
          name: volume
      volumes:
      - name: volume
        persistentVolumeClaim:
          claimName: mydata-nfs-rwx
