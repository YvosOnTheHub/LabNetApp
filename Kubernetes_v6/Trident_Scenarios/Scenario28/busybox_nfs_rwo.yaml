apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mydata-nfs-rwo
  namespace: scenario28
  labels:
    app.kubernetes.io/name: scenario28
    app: bbox-nfs-rwo
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: storage-class-nfs
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: busybox-nfs-rwo
  namespace: scenario28
  labels:
    app.kubernetes.io/name: scenario28
    app: bbox-nfs-rwo
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: scenario28
      app: bbox-nfs-rwo
  template:
    metadata:
      labels:
        app.kubernetes.io/name: scenario28
        app: bbox-nfs-rwo
    spec:
      nodeSelector:
        kubernetes.io/os: linux
      containers:
      - name: busybox
        image: registry.demo.netapp.com/busybox:1.35.0
        imagePullPolicy: IfNotPresent
        command: ['sleep', '3600']        
        volumeMounts:
        - mountPath: /data
          name: volume
      volumes:
      - name: volume
        persistentVolumeClaim:
          claimName: mydata-nfs-rwo
