apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-nfs
  namespace: share
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: storage-class-nfs
  volumeMode: Filesystem
