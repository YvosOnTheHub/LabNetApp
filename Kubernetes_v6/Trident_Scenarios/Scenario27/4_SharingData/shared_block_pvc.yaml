apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-block
  namespace: share
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: storage-class-iscsi
  volumeMode: Block
