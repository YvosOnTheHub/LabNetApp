apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc1
  labels:
    app.kubernetes.io/name: test1
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: storage-class-nfs
