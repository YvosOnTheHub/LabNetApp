apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quotasc-3
  labels:
      scenario: quotas
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: storage-class-nas