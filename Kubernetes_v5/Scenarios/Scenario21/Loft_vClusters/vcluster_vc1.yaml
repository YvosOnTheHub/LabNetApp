vcluster:
  resources:
    limits:
      memory: 512Mi
  
sync:
  nodes:
    enabled: true
    nodeSelector: "tenant1=true"
  volumesnapshots:
    enabled: true

syncer:
  resources:
    limits:
      memory: 512Mi