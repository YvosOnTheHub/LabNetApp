kind: Pod
apiVersion: v1
metadata:
  name: busybox-pod5
spec:
  volumes:
    - name: volume
      persistentVolumeClaim:
       claimName: pvc5
  containers:
    - name: debugger
      image: busybox
      imagePullPolicy: IfNotPresent
      command: ['sleep', '3600']
      volumeMounts:
        - mountPath: "/data"
          name: volume