apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ontaplimit-2
  labels:
      scenario: ontap
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: storage-class-nas